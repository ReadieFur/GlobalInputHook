namespace GlobalInputHook
{
    public enum EExitCodes
    {
        Normal = 0,
        InvalidParentProcessID,
        InvalidMapArgument,
        InvalidUpdateRateArgument,
        ParentProcessExited
    }
}
